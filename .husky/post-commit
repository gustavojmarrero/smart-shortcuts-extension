#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "📦 Creating release package..."
node scripts/package-release.cjs

echo "🚀 Creating GitHub Release..."
node scripts/create-github-release.cjs

echo "✅ All done!"
